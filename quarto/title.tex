$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
$endif$

$if(subtitle)$
\subtitle{$subtitle$}
$endif$
$for(by-author)$
\usepackage{academicons}
\usepackage{xcolor}

  \author{$by-author.name.literal$}
  $if(by-author.affiliations)$
    $for(by-author.affiliations)$
      \affil{%
        $if(by-author.affiliations.name)$
     \small{     $by-author.affiliations.name$
          ORCID \textcolor[HTML]{A6CE39}{\aiOrcid} ~$by-author.orcid$ }
        $endif$
      }
    $endfor$
  $endif$
$endfor$



\date{$date$}